import React from "react";
import { createRoot } from "react-dom/client";
import Rumoar from "./Rumoar.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Rumoar />
  </React.StrictMode>
);
